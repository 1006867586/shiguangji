# 飨刻 (XiangKe) — 团体聚餐记录应用

## 项目规划文件 (PRD + 技术规格)

> 适用：AI 编程助手上下文 | 版本：v1.0 | 日期：2026-07-25

---

## 1. 项目概述

**产品定位**：一个面向小团体（3-20人）的私密聚餐记录应用，主界面类似微信朋友圈，仅团体成员可见。成员可发起聚餐活动、分享美团/大众点评链接、补充活动照片、评论与转发。

**目标用户**：固定小团体（室友、同事、老友群、家庭群）。

**核心体验**：
- 打开即见团体 feed 流，按时间倒序排列
- 发起活动时可选「纯文字自定义」或「粘贴美团/点评链接自动解析」
- 活动发布后，任何成员均可追加照片，形成集体相册
- 支持评论、转发（带附言）

---

## 2. 技术栈

| 层级 | 技术选型 | 说明 |
|------|---------|------|
| 前端框架 | **Next.js 15** (App Router) + TypeScript | EdgeOne Pages 原生全栈支持 |
| UI 组件 | Tailwind CSS + shadcn/ui | 快速构建朋友圈风格界面 |
| 数据库 | **Supabase** (PostgreSQL) | Auth + DB + Realtime 一体化 |
| 对象存储 | **Cloudflare R2** | 图片存储（配合预签名 URL 上传） |
| 部署平台 | **EdgeOne Pages** | 全栈部署（SSR + API Routes + Edge Functions） |
| 实时推送 | Supabase Realtime | feed 更新、新评论、新照片实时同步 |

---

## 3. 数据库设计（Supabase PostgreSQL）

### 3.1 Schema 定义

```sql
-- 启用 UUID 扩展
create extension if not exists "uuid-ossp";

-- ========== 用户资料表（扩展 Supabase Auth） ==========
create table public.profiles (
  id uuid references auth.users on delete cascade primary key,
  nickname text not null,
  avatar_url text,
  created_at timestamptz default now()
);

-- ========== 团体表 ==========
create table public.groups (
  id uuid default uuid_generate_v4() primary key,
  name text not null,
  description text,
  avatar_url text,
  invite_code text unique not null, -- 6位随机字母数字，用于邀请加入
  created_by uuid references auth.users not null,
  created_at timestamptz default now()
);

-- ========== 团体成员关系表 ==========
create table public.group_members (
  id uuid default uuid_generate_v4() primary key,
  group_id uuid references public.groups on delete cascade not null,
  user_id uuid references auth.users on delete cascade not null,
  role text not null default 'member' check (role in ('admin', 'member')),
  joined_at timestamptz default now(),
  unique (group_id, user_id)
);

-- ========== 活动/动态表（朋友圈核心） ==========
create table public.activities (
  id uuid default uuid_generate_v4() primary key,
  group_id uuid references public.groups on delete cascade not null,
  author_id uuid references auth.users not null,

  -- 类型：原创活动 | 转发
  type text not null default 'original' check (type in ('original', 'repost')),

  -- 文字内容
  content text,

  -- 外部链接信息（美团/点评等）
  external_link jsonb, -- { platform, title, url, cover_image, rating, address, price }

  -- 转发相关
  repost_of_id uuid references public.activities on delete set null,
  repost_comment text, -- 转发时的附言

  created_at timestamptz default now(),
  updated_at timestamptz default now()
);

-- ========== 活动照片表（成员补充上传） ==========
create table public.activity_photos (
  id uuid default uuid_generate_v4() primary key,
  activity_id uuid references public.activities on delete cascade not null,
  uploaded_by uuid references auth.users not null,
  url text not null, -- Cloudflare R2 公开访问 URL
  caption text, -- 照片说明
  created_at timestamptz default now()
);

-- ========== 评论表 ==========
create table public.comments (
  id uuid default uuid_generate_v4() primary key,
  activity_id uuid references public.activities on delete cascade not null,
  author_id uuid references auth.users not null,
  content text not null,
  parent_id uuid references public.comments on delete cascade, -- 楼中楼回复
  created_at timestamptz default now()
);

-- ========== 点赞表（可选但建议实现） ==========
create table public.activity_likes (
  id uuid default uuid_generate_v4() primary key,
  activity_id uuid references public.activities on delete cascade not null,
  user_id uuid references auth.users on delete cascade not null,
  created_at timestamptz default now(),
  unique (activity_id, user_id)
);

-- ========== 索引优化 ==========
create index idx_activities_group_created on public.activities(group_id, created_at desc);
create index idx_activity_photos_activity on public.activity_photos(activity_id);
create index idx_comments_activity on public.comments(activity_id, created_at);
create index idx_group_members_user on public.group_members(user_id);
create index idx_group_members_group on public.group_members(group_id);
```

### 3.2 Row Level Security (RLS) 策略

**核心原则**：用户只能访问其所属团体的数据。

```sql
-- 为所有表启用 RLS
alter table public.profiles enable row level security;
alter table public.groups enable row level security;
alter table public.group_members enable row level security;
alter table public.activities enable row level security;
alter table public.activity_photos enable row level security;
alter table public.comments enable row level security;
alter table public.activity_likes enable row level security;

-- ========== profiles：所有人可读，仅自己可改 ==========
create policy "Profiles are viewable by everyone"
  on public.profiles for select using (true);

create policy "Users can update own profile"
  on public.profiles for update using (auth.uid() = id);

-- ========== groups：成员可读，创建者可改 ==========
create policy "Group members can view group"
  on public.groups for select
  using (exists (
    select 1 from public.group_members gm
    where gm.group_id = groups.id and gm.user_id = auth.uid()
  ));

create policy "Group creator can update"
  on public.groups for update
  using (created_by = auth.uid());

-- ========== group_members：同团体成员互见 ==========
create policy "Members visible to groupmates"
  on public.group_members for select
  using (exists (
    select 1 from public.group_members gm
    where gm.group_id = group_members.group_id and gm.user_id = auth.uid()
  ));

create policy "Users can join via invite"
  on public.group_members for insert
  with check (user_id = auth.uid());

-- ========== activities：团体成员 CRUD ==========
create policy "Activities viewable by group members"
  on public.activities for select
  using (exists (
    select 1 from public.group_members gm
    where gm.group_id = activities.group_id and gm.user_id = auth.uid()
  ));

create policy "Group members can create activities"
  on public.activities for insert
  with check (exists (
    select 1 from public.group_members gm
    where gm.group_id = activities.group_id and gm.user_id = auth.uid()
  ));

create policy "Authors can update/delete own activities"
  on public.activities for all
  using (author_id = auth.uid());

-- ========== activity_photos / comments / likes：同上逻辑 ==========
-- （策略模式与 activities 一致，基于 group_id 关联判断成员身份）
```

---

## 4. API 设计（Next.js Route Handlers）

### 4.1 接口清单

| 方法 | 路径 | 说明 |
|------|------|------|
| `GET` | `/api/feed` | 获取团体 feed 流（分页） |
| `POST` | `/api/activities` | 发起新活动 |
| `GET` | `/api/activities/[id]` | 获取活动详情 |
| `DELETE` | `/api/activities/[id]` | 删除活动 |
| `POST` | `/api/activities/[id]/photos` | 追加活动照片（仅记录 URL） |
| `DELETE` | `/api/activities/[id]/photos/[photoId]` | 删除照片 |
| `POST` | `/api/activities/[id]/comments` | 发表评论 |
| `DELETE` | `/api/activities/[id]/comments/[commentId]` | 删除评论 |
| `POST` | `/api/activities/[id]/repost` | 转发活动 |
| `POST` | `/api/activities/[id]/like` | 点赞/取消点赞 |
| `GET` | `/api/groups` | 获取当前用户所属团体 |
| `POST` | `/api/groups` | 创建新团体 |
| `POST` | `/api/groups/join` | 通过邀请码加入团体 |
| `POST` | `/api/upload/presign` | 获取 Cloudflare R2 预签名上传 URL |

### 4.2 关键接口详情

#### `GET /api/feed?groupId=<uuid>&cursor=<uuid>&limit=20`

**响应结构**：
```json
{
  "data": [
    {
      "id": "uuid",
      "type": "original",
      "content": "今晚吃火锅！",
      "external_link": null,
      "created_at": "2026-07-25T12:00:00Z",
      "author": { "id": "uuid", "nickname": "小明", "avatar_url": "..." },
      "photos": [{ "id": "uuid", "url": "...", "uploaded_by": "..." }],
      "photo_count": 5,
      "comment_count": 3,
      "like_count": 8,
      "is_liked": true,
      "repost_of": null
    }
  ],
  "next_cursor": "uuid-or-null"
}
```

**实现要点**：
- 使用 Supabase RPC 或复杂查询一次性 JOIN profiles、activity_photos（聚合计数）、comments（聚合计数）、likes
- 按 `created_at desc` 分页，使用 `cursor` 而非 `offset` 避免数据漂移

#### `POST /api/activities`

**请求体**：
```json
{
  "groupId": "uuid",
  "content": "今晚去吃这家！",
  "externalLink": {
    "platform": "dianping",
    "url": "https://www.dianping.com/shop/xxx",
    "title": "海底捞火锅",
    "coverImage": "https://...",
    "rating": 4.8,
    "address": "..."
  }
}
```

**外部链接解析**：
- 若用户粘贴 URL，后端使用 `link-preview-js` 或 `cheerio` 解析 Open Graph 元数据
- 若解析失败，允许用户手动填写 `title` 和 `coverImage`

#### `POST /api/upload/presign`

**流程**：
1. 前端请求预签名 URL，携带 `filename`、`contentType`
2. 后端使用 Cloudflare R2 S3 API 生成 `PUT` 预签名 URL（有效期 5 分钟）
3. 前端直接 `PUT` 到 R2，成功后拿到最终 URL
4. 前端再调用 `POST /api/activities/[id]/photos` 将 URL 写入 Supabase

---

## 5. 前端架构

### 5.1 页面路由

```
/                     # 主 Feed 页（默认进入最近活跃的团体）
/g/[groupId]          # 指定团体的 Feed 页
/activity/[id]        # 活动详情页（照片大图、评论列表）
/new                  # 发起活动页
/profile              # 个人中心
/groups/new           # 创建团体
/join?code=xxx        # 通过邀请码加入团体
```

### 5.2 核心组件

| 组件 | 职责 |
|------|------|
| `FeedCard` | 朋友圈卡片：头像、昵称、时间、文字内容、外部链接卡片、照片网格、操作栏（点赞/评论/转发） |
| `PhotoGrid` | 照片网格布局：1张全宽、2张并排、3-9张九宫格，点击放大查看 |
| `ExternalLinkCard` | 美团/点评链接卡片：展示封面图、标题、评分、地址 |
| `CommentSection` | 评论区：输入框、评论列表、楼中楼回复 |
| `PhotoUploader` | 活动详情页内的照片补充上传组件（多选、压缩、直传 R2） |
| `ActivityForm` | 发起活动表单：文字输入、外部链接粘贴框（带解析 loading）、团体选择 |

### 5.3 实时同步

使用 Supabase Realtime 订阅以下变更：
- `activities`：新活动发布时，自动 prepend 到 feed 顶部
- `activity_photos`：有新照片补充时，更新对应卡片的 photo_count 和缩略图
- `comments`：新评论实时显示在对应活动下

```typescript
// lib/supabase/realtime.ts
export function subscribeToGroup(groupId: string, callbacks: { ... }) {
  const channel = supabase
    .channel(`group-${groupId}`)
    .on('postgres_changes', { event: '*', schema: 'public', table: 'activities', filter: `group_id=eq.${groupId}` }, callbacks.onActivity)
    .on('postgres_changes', { event: 'INSERT', schema: 'public', table: 'activity_photos' }, callbacks.onPhoto)
    .on('postgres_changes', { event: 'INSERT', schema: 'public', table: 'comments' }, callbacks.onComment)
    .subscribe();
  return channel;
}
```

---

## 6. 图片存储方案（Cloudflare R2）

### 6.1 架构决策

不直接使用 Supabase Storage，而是使用 **Cloudflare R2**：
- 原因：用户指定 + R2 无 egress 费用，适合图片分享场景
- 公开访问：R2 Bucket 绑定自定义域名（如 `img.xiangke.app`），图片 URL 可直接访问

### 6.2 上传流程

```
前端选择图片
  → 浏览器端压缩（browser-image-compression，限制单张 < 3MB）
  → POST /api/upload/presign { filename: "uuid.jpg", contentType: "image/jpeg" }
  ← 返回 { presignedUrl, publicUrl }
  → PUT presignedUrl (图片二进制)
  ← 200 OK
  → POST /api/activities/[id]/photos { url: publicUrl, caption?: "" }
  ← 写入 Supabase 成功，Realtime 推送更新
```

### 6.3 R2 配置要求

- Bucket 名称：`xiangke-images`
- 公开访问：启用，绑定自定义域名
- CORS 配置：允许 `PUT` 来自前端域名
- 生命周期：可选，旧图片自动归档

---

## 7. 部署方案（EdgeOne Pages）

### 7.1 构建配置

```typescript
// next.config.ts
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // EdgeOne Pages 支持 SSR/ISR/SSG，无需 static export
  images: {
    remotePatterns: [
      { hostname: "*.r2.cloudflarestorage.com" },
      { hostname: "img.xiangke.app" }, // 你的 R2 自定义域名
    ],
  },
};

export default nextConfig;
```

### 7.2 环境变量（EdgeOne 控制台配置）

| 变量名 | 说明 |
|--------|------|
| `NEXT_PUBLIC_SUPABASE_URL` | Supabase Project URL |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | Supabase anon public key |
| `SUPABASE_SERVICE_ROLE_KEY` | Supabase service role key（服务端用） |
| `R2_ACCOUNT_ID` | Cloudflare Account ID |
| `R2_ACCESS_KEY_ID` | R2 API Token Access Key |
| `R2_SECRET_ACCESS_KEY` | R2 API Token Secret Key |
| `R2_BUCKET_NAME` | R2 Bucket 名称 |
| `R2_PUBLIC_URL` | R2 自定义域名，如 `https://img.xiangke.app` |

### 7.3 部署命令

```bash
# 安装 EdgeOne CLI
npm install -g edgeone

# 登录
edgeone login

# 初始化项目（首次）
edgeone pages init

# 部署
edgeone pages deploy
```

---

## 8. 项目目录结构

```
xiangke/
├── app/
│   ├── (main)/
│   │   ├── layout.tsx              # 主布局：底部导航/侧边栏
│   │   ├── page.tsx                # 首页：默认团体 Feed
│   │   ├── g/[groupId]/page.tsx    # 指定团体 Feed
│   │   ├── activity/[id]/page.tsx  # 活动详情页
│   │   ├── new/page.tsx            # 发起活动
│   │   └── profile/page.tsx        # 个人中心
│   ├── join/page.tsx               # 邀请码加入页
│   ├── api/
│   │   ├── feed/route.ts
│   │   ├── activities/route.ts
│   │   ├── activities/[id]/route.ts
│   │   ├── activities/[id]/photos/route.ts
│   │   ├── activities/[id]/comments/route.ts
│   │   ├── activities/[id]/repost/route.ts
│   │   ├── activities/[id]/like/route.ts
│   │   ├── groups/route.ts
│   │   ├── groups/join/route.ts
│   │   └── upload/presign/route.ts
│   ├── layout.tsx
│   └── globals.css
├── components/
│   ├── ui/                         # shadcn/ui 组件
│   ├── feed/FeedCard.tsx
│   ├── feed/FeedList.tsx
│   ├── feed/CommentSection.tsx
│   ├── activity/ActivityForm.tsx
│   ├── activity/PhotoGrid.tsx
│   ├── activity/PhotoUploader.tsx
│   ├── activity/ExternalLinkCard.tsx
│   └── group/GroupSelector.tsx
├── hooks/
│   ├── useFeed.ts
│   ├── useActivity.ts
│   ├── useRealtime.ts
│   └── useUpload.ts
├── lib/
│   ├── supabase/
│   │   ├── client.ts               # 浏览器端 client（带 auth）
│   │   ├── server.ts               # Server Component / API Route 用
│   │   └── admin.ts                # Service Role（绕过 RLS）
│   ├── r2.ts                       # R2 S3 Client + 预签名逻辑
│   ├── utils.ts
│   └── constants.ts
├── types/
│   └── index.ts                    # 全局 TypeScript 类型
├── supabase/
│   └── migrations/
│       └── 001_init.sql            # 数据库初始化 SQL
├── public/
├── next.config.ts
├── tailwind.config.ts
├── tsconfig.json
└── package.json
```

---

## 9. 关键实现细节与约束

### 9.1 外部链接解析（美团/大众点评）

**方案**：
- 用户粘贴 URL → 后端调用 `link-preview-js` 获取 Open Graph 标签
- **Fallback**：若平台有反爬，前端提供手动编辑表单（标题、封面图、评分、地址）

**代码示意**：
```typescript
// app/api/activities/route.ts
import { getLinkPreview } from 'link-preview-js';

async function parseExternalLink(url: string) {
  try {
    const preview = await getLinkPreview(url, { timeout: 5000 });
    return {
      platform: url.includes('dianping') ? 'dianping' : 'meituan',
      title: preview.title,
      url,
      coverImage: preview.images?.[0],
      // 评分和地址需要更精细的爬虫或用户手动补充
    };
  } catch {
    return null;
  }
}
```

### 9.2 Feed 查询优化

使用 Supabase 的 `rpc` 调用自定义 PostgreSQL 函数，避免 N+1 查询：

```sql
-- 创建 feed 查询函数
create or replace function get_group_feed(
  p_group_id uuid,
  p_cursor timestamptz,
  p_limit int
)
returns table (
  id uuid,
  type text,
  content text,
  external_link jsonb,
  created_at timestamptz,
  author jsonb,
  photo_count bigint,
  comment_count bigint,
  like_count bigint,
  is_liked boolean,
  repost_of jsonb
)
language sql stable
as $$
  select
    a.id,
    a.type,
    a.content,
    a.external_link,
    a.created_at,
    jsonb_build_object('id', p.id, 'nickname', p.nickname, 'avatar_url', p.avatar_url) as author,
    (select count(*) from public.activity_photos ap where ap.activity_id = a.id) as photo_count,
    (select count(*) from public.comments c where c.activity_id = a.id) as comment_count,
    (select count(*) from public.activity_likes l where l.activity_id = a.id) as like_count,
    false as is_liked, -- 需在应用层根据当前用户补充
    case when a.repost_of_id is not null then
      (select jsonb_build_object('id', ra.id, 'content', ra.content, 'author', jsonb_build_object('nickname', rp.nickname))
       from public.activities ra join public.profiles rp on ra.author_id = rp.id
       where ra.id = a.repost_of_id)
    else null end as repost_of
  from public.activities a
  join public.profiles p on a.author_id = p.id
  where a.group_id = p_group_id
    and a.created_at < coalesce(p_cursor, now())
  order by a.created_at desc
  limit p_limit;
$$;
```

### 9.3 邀请码机制

- 创建团体时生成 6 位随机字符串（A-Z, 0-9，排除易混淆字符）
- 用户访问 `/join?code=XXX` 或输入邀请码 → 调用 `POST /api/groups/join`
- 加入后自动写入 `group_members`

### 9.4 安全与隐私

- **RLS 是核心防线**：所有数据查询必须通过 RLS 验证团体成员身份
- **图片访问**：R2 图片 URL 虽然是公开的，但 URL 包含随机 UUID，难以猜测
- **敏感操作**：删除活动/照片/评论仅限作者本人或团体管理员

---

## 10. MVP 功能优先级

| 优先级 | 功能 | 说明 |
|--------|------|------|
| P0 | 团体创建与加入 | 邀请码机制 |
| P0 | 发起活动（文字+外部链接） | 朋友圈核心 |
| P0 | Feed 流展示 | 分页加载 |
| P0 | 照片补充上传 | Cloudflare R2 直传 |
| P0 | 评论 | 一级评论即可 |
| P1 | 转发 | 带附言的转发 |
| P1 | 点赞 | 点赞/取消点赞 |
| P1 | 实时同步 | Supabase Realtime |
| P2 | 楼中楼评论 | 二级回复 |
| P2 | 活动提醒/通知 | 新活动推送 |
| P2 | 图片压缩/水印 | 前端压缩 + 可选水印 |

---

## 11. 交付检查清单

- [ ] Supabase 项目创建，执行 `001_init.sql`
- [ ] Cloudflare R2 Bucket 创建，配置公开访问 + CORS
- [ ] EdgeOne Pages 项目初始化，配置环境变量
- [ ] Next.js 项目搭建，shadcn/ui 初始化
- [ ] 认证流程：登录/注册 → 创建/加入团体 → 进入 Feed
- [ ] 核心链路跑通：发起活动 → 上传照片 → 评论 → 转发
- [ ] 移动端适配（朋友圈场景以移动端为主）

---

*文档版本：v1.0 | 生成日期：2026-07-25*
